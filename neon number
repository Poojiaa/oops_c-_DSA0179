#include<iostream>
using namespace std;
int main()
{
	int n,r,sum=0,m;
	cout<<"enter the number"<<" ";
	cin>>n;
	int square=n*n;
	while(square!=0)
	{
		r=square%10;
		
		sum=sum+r;
		square=square/10;
	}
	if(n%sum==0)
	cout<<"it is a neon number";
	else
	cout<<"it is not a neon number";
}
		
