#include<iostream>
using namespace std;
int main()
{
	int n,r,sum=0,m;
	cout<<"enter the number"<<" ";
	cin>>n;
	m=n;
	while(n!=0)
	{
		r=n%10;
		sum=sum+r;
		n=n/10;
		
	}
	if(m%sum==0)
	
		cout<<"it is a harshad number";
	else
	cout<<"it is not a harshad number";	
	
	
}
